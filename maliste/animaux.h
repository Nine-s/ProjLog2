#include <stdio.h>
#include <string.h>
#include <stdlib.h>

typedef  struct Animal * ptrStructAnimal; // permet de définir un alisas pour le pointeur sur la struct animale

extern ptrStructAnimal creerUnAnimal(ptrStructAnimal);
